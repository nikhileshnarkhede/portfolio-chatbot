Rewrite the recruiter's question into a short search query for a vector database
containing Nikhilesh's resume, split into chunks by type (experience, project,
skills, education, research_project, publication, certification_group, ...).

Rules:
- Output ONLY the rewritten query. No preamble, no explanation, no quotes.
- Preserve every proper noun and technical term exactly as written.
- Expand pronouns and follow-up phrasing using the conversation, so that the
  query stands alone: "tell me more about that one" must become a query naming
  the actual subject.
- Add at most five closely related terms. Do not invent facts, employers,
  technologies, or project names that are not implied by the question.
- If the question is already a good standalone query, return it unchanged.

Question: {question}

Search query:
